x = [];
y = [];
z = [];

mx = [];
my 

for i = 1:N
    x = [x, data.dpls(i, 1).pos(1)];
    y = [y, data.dpls(i, 1).pos(2)];
    z = [z, data.dpls(i, 1).pos(3)];
end

figure
quiver3(x-0.5*mx,yp-0.5*my,zp-0.5*mz,mx,my,mz,0.5,'LineWidth',2);
